import styled from 'styled-components/macro';

export const HeaderSection = styled.section`
  position: fixed;
  top: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 100%;
  background: linear-gradient(0deg, rgba(0, 0, 0, 0.25) 0%, #504C4C 100%);
  z-index: 1000;
`

export const HeaderSection = styled.section`
  position: fixed;
  top: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 100%;
  background: linear-gradient(0deg, rgba(0, 0, 0, 0.25) 0%, #504C4C 100%);
  z-index: 1000;
`

export const HeaderStyled = styled.header`
  padding: 10px 0;
`;
